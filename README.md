
  # Horizontal Scroll Carousel

  This is a code bundle for Horizontal Scroll Carousel. The original project is available at https://www.figma.com/design/LEgLfcUho9ynbJrf2vfJu8/Horizontal-Scroll-Carousel.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  